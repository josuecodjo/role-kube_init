---
apiVersion: awx.ansible.com/v1beta1
kind: AWX
metadata:
  name: awx
spec:

  admin_user: admin
  admin_password_secret: awx-admin-password

  ingress_type: ingress
  ingress_hosts:
    - hostname: {{ awx_hostname }}.{{ global_dns_name }}
      tls_secret: {{ awx_hostname }}.{{ global_dns_name }}
  ingress_annotations: |
    cert-manager.io/issuer: awx-issuer
    cert-manager.io/propagation-timeout: "30s"
    cert-manager.io/polling-interval: "5s"
    # kubernetes.io/ingress.class: traefik

  postgres_configuration_secret: awx-postgres-configuration

  postgres_data_volume_init: true
  postgres_storage_class: awx-postgres-volume
  postgres_storage_requirements:
    requests:
      storage: {{ postgres_storage }} # 8Gi

  projects_persistence: true
  projects_existing_claim: awx-projects-claim

  web_replicas: {{ web_replicas }} # 1
  task_replicas: {{ task_replicas }} # 1

  web_resource_requirements: {}
  task_resource_requirements: {}
  ee_resource_requirements: {}
  init_container_resource_requirements: {}
  postgres_resource_requirements: {}
  redis_resource_requirements: {}
  rsyslog_resource_requirements: {}

  # Uncomment to reveal "censored" logs
  #no_log: false
